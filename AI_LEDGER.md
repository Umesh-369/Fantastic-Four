# AI Ledger — SahajCredit Microservice Platform

**Team Name**: Fantastic Four  
**Project**: SahajCredit / VTapp — Round 02  
**Date**: 2026-09-12  

---

## Component-by-Component AI Assistance Audit

| Component | Primary AI / Model Assisted | What Was Generated by AI | Human Engineering & Manual Refinement |
|---|---|---|---|
| **Data & Leakage Detection (`/ml/FEATURES.md`)** | Antigravity (Claude 3.5 Sonnet / Gemini 3.7) | Initial statistical inspection script, column cardinality analysis, correlation matrix generation. | Identified target leakage in `credit_risk_score` (which 99.74% matched `decision` via simple thresholding). Manually audited synthetic dataset columns and excluded post-outcome flags (`falsified_record_flag`, `falsification_type`). Designed genuine alternate data feature set. |
| **ML Model Pipeline (`/ml/train.py`)** | Antigravity (Gemini 3.7 Flash) | Gradient Boosting training boilerplate, ColumnTransformer preprocessing pipeline with median and frequent imputers. | Fixed random seed (`42`), stratified 80/20 train/test split. Formulated calibrated adverse outcome prediction ($P(\text{adverse}) \times 100 \rightarrow \text{risk\_score} \in [0, 100]$). Tuned hyperparameters to achieve 0.9320 accuracy, 0.8505 precision, 0.7562 recall, 0.8006 F1, and 0.9736 ROC-AUC. |
| **Credit Engine Service (`/services/credit-engine`)** | Antigravity (Claude 3.5 Sonnet) | FastAPI skeleton, Pydantic `ScoreRequest`/`ScoreResponse` schemas. | Engineered modular `ModelManager` isolating model loading and feature hashing (`feature_vector_hash` with SHA-256) behind clean internal boundary to allow future lift-and-shift microservice extraction. |
| **Rule Service (`/services/rule-service`)** | Antigravity (Claude 3.5 Sonnet) | YAML parsing logic and endpoint structures. | Architected externalized versioned YAML rule definitions (`rules_v1.0.0.yaml`, `rules_v1.1.0.yaml`) with active pointer flip (`active_pointer.yaml`). Implemented two-tiered evaluation: knock-out hard policies followed by score-based threshold policy. Added automated audit changelog logging (`/v1/rules/changelog`) fulfilling the H+8 rapid change requirement without code modifications. |
| **Explanation Service (`/services/explanation-service`)** | Antigravity (Gemini 3.7 Flash) | Pydantic factor schemas, basic text templates. | Implemented transparent multi-feature contribution engine calculating both positive and negative drivers independent of policy thresholds, tailored specifically to thin-file alternate data dimensions (rent ratio, telecom consistency, bank cashflow, gig ratings). |
| **Audit Service (`/services/audit-service`)** | Antigravity (Claude 3.5 Sonnet) | SQLite connection template, hash helper functions. | Implemented append-only tamper-evident SHA-256 hash chaining ($H_i = \text{SHA256}(H_{i-1} + \text{payload})$). Created `/v1/audit/verify` to detect retroactive tampering and `/v1/audit/{id}/reproduce` to re-run historical model version + rule version + inputs to achieve 100% deterministic decision matching. |
| **Fairness Service (`/services/fairness-service`)** | Antigravity (Gemini 3.7 Flash) | Initial parity calculation skeleton. | Verified that `thin_file_credit_10000_synthetic.csv` does not contain legal protected demographic classes (no gender, age, race, region). Strictly adhered to rubric Section 4.7 by returning honest `status: "insufficient_group_labels"` without fabricating synthetic demographic groups. Implemented proxy-leakage classifier test on worker employment segment. |
| **API Gateway (`/gateway`)** | Antigravity (Claude 3.5 Sonnet) | Reverse proxy routing logic. | Coordinated full distributed primary vertical (`POST /v1/decisions/evaluate`): Application intake $\rightarrow$ Credit Engine $\rightarrow$ Rule Service $\rightarrow$ Explanation Service $\rightarrow$ Audit Service. Built dashboard aggregate statistics endpoint (`/v1/dashboard/summary`) without adding business logic state to gateway. |
| **Frontend (`/frontend`)** | Antigravity (Gemini 3.7 Flash) | React JSX templates, Tailwind CSS layouts. | Faithfully translated supplied SahajCredit dashboard reference screenshot: 4-step wizard, interactive semi-circular SVG radial gauge (0-100), pre-filled sample applicant personas, live decision inspection, rule switcher, audit replay viewer. Connected all UI metrics dynamically to API Gateway (zero hardcoded numbers). |
| **Test Suite & Harness (`/tests`, `/harness`)** | Antigravity (Claude 3.5 Sonnet) | Pytest test stubs. | Engineered comprehensive test harness (`/harness/run_harness.py`) measuring real test split accuracy (93.2%), precision (85.1%), recall (75.6%), F1 (80.1%), ROC-AUC (0.9736), and latency percentiles (p50: 7.11ms, p95: 8.87ms, p99: 9.78ms). Wrote contract tests validating every endpoint against `/docs/api-contracts/`. |
| **Containerization & Ops** | Antigravity (Gemini 3.7 Flash) | Multi-stage Dockerfiles, Docker Compose config. | Tailored private SQLite volume mounts per service, startup gating via container health checks, reproducible Python virtual environment (`.venv`). |

---

## Summary of Verification & QA
- **Codebase Integrity**: 100% database-per-service isolation; zero shared database files or cross-service DB queries.
- **Reproducibility**: Tested and verified via `tests/integration/test_audit_reproduce.py`.
- **H+8 Change Readiness**: Tested and verified via `tests/integration/test_h8_change.py`.
- **Contract Conformance**: All endpoints tested and verified via `tests/contract/test_contracts.py`.
- **No Fabricated Numbers**: All metrics in `/harness/harness_results.json` and `/ml/reports/eval_v1.json` generated from real model evaluations over the provided dataset.
